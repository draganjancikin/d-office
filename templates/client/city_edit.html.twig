{% extends 'base.html.twig' %}

{% block title %}Edit City{% endblock %}

{% block body %}
    {% include 'client/tools_menu.html.twig' %}
    <div class="col-sm-12 col-md-11 col-lg-9 col-xl-7 px-2">
        <div class="card mb-4">
            <div class="card-header p-2">Izmena podataka o Naselju:</div>
            <div class="card-body">
                {% include 'client/form/city.html.twig' %}
            </div>
        </div>
    </div>

{#    <h1>Edit City</h1>#}
{#    {{ form_start(form) }}#}
{#        {{ form_widget(form) }}#}
{#        <button class="btn btn-primary" type="submit">Save</button>#}
{#        <a href="{{ path('city_show', {'id': city.id}) }}" class="btn btn-secondary">Cancel</a>#}
{#    {{ form_end(form) }}#}
{% endblock %}

